# MyNovel — 小说创作助手

Flask + SQLite 小说创作辅助工具。书架/人物/时间线/世界观 SPA。

## 铁律

1. **每一步前后都要 git commit。** 不允许连续多步修改后再提交。提交信息用中文。
2. **用户提修改意见时，先出方案和探讨，不要直接动手改。** 用户确认后再执行。
3. 用户要求说中文。

## 技术栈

- Flask + SQLAlchemy + SQLite
- Jinja2 模板（base.html）
- 前端 Vanilla JS，无框架
- CSS 黑白灰配色（变量见 style.css `:root`）
